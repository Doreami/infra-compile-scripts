# SIFT1M 火焰图分析 — YYYY-MM-DD

## 版本信息

<!-- 采集后填写: 在各仓库 git log --oneline -1 -->
| 组件 | Commit | 说明 |
|------|--------|------|
| openGauss-server-datainfra | `XXXXXXX` | |
| iceberg-rust-bridge | `XXXXXXX` | |
| iceberg-index | `XXXXXXX` | |
| iceberg-rust-cache | `XXXXXXX` | |
| iceberg-rust-datainfra | `XXXXXXX` | Iceberg Rust SDK |
| openGauss-Catalog | `XXXXXXX` | |
| iceberg_fdw | `XXXXXXX` | |
| iceberg_delta | `XXXXXXX` | |
| infra-compile-scripts | `XXXXXXX` | 测试脚本 |
| Kernel | `uname -r` | |

## 采集信息

| 项目 | 值 |
|------|---|
| 采样工具 | `perf record -F 99 -g -p <gaussdb PID>` |
| 编译模式 | Release + 帧指针 (`-fno-omit-frame-pointer`) |
| 数据集 | SIFT1M, 1M 行 × 128 维 float, 10 个 Parquet 文件 |
| 向量索引 | ivf_flat, num_clusters=1024 (或实际值) |

## 场景覆盖

<!-- 从 flamegraphs/YYYY-MM-DD/README.txt 取数据 -->

| 场景 | 样本数 | 总耗时 | 采样策略 | 说明 |
|------|--------|--------|----------|------|
| `fullscan_k10` | | | 2 轮 | 全扫 LIMIT 10 |
| `fullscan_k100` | | | 2 轮 | 全扫 LIMIT 100 |
| `fullscan_k1000` | | | 2 轮 | 全扫 LIMIT 1000 |
| `ivf_k10` | | | 5 轮 | IVF 索引, K=10 |
| `ivf_k100` | | | 3 轮 | IVF 索引, K=100 |
| `ivf_k10000` | | | 2 轮 | IVF 索引, K=10000 |
| `btree_point` | | | 60 轮 | Btree 点查 WHERE id=500000 |

---

## 一、逐场景详细数据

<!-- 每个场景执行: perf report -i perf_<场景>_YYYY-MM-DD.data --stdio --percent-limit 0.5 --no-children -->

### 1.1 全扫 K=10

```sql
SET enable_indexscan = off; SET enable_bitmapscan = off; SET enable_vectorsearch = off;
SELECT id FROM sift_ns.sift1m ORDER BY vec <-> '<常量>'::vector LIMIT 10;
```

| 排名 | 占比 | Shared Object | 函数 | 分类 |
|:----:|:----:|---------------|------|:----:|
| 1 | | | | |
| 2 | | | | |
| 3 | | | | |
| 4 | | | | |
| 5 | | | | |

### 1.2 全扫 K=100

| 排名 | 占比 | Shared Object | 函数 | 分类 |
|:----:|:----:|---------------|------|:----:|
| | | | | |

### 1.3 全扫 K=1000

| 排名 | 占比 | Shared Object | 函数 | 分类 |
|:----:|:----:|---------------|------|:----:|
| | | | | |

### 1.4 IVF K=10

```sql
SET enable_vectorsearch = on; SET try_vector_engine_strategy = force;
SELECT id FROM sift_ns.sift1m ORDER BY vec <-> '<常量>'::vector LIMIT 10;
```

| 排名 | 占比 | Shared Object | 函数 | 分类 |
|:----:|:----:|---------------|------|:----:|
| | | | | |

### 1.5 IVF K=100

| 排名 | 占比 | Shared Object | 函数 | 分类 |
|:----:|:----:|---------------|------|:----:|
| | | | | |

### 1.6 IVF K=10000

| 排名 | 占比 | Shared Object | 函数 | 分类 |
|:----:|:----:|---------------|------|:----:|
| | | | | |

### 1.7 Btree 点查

```sql
SELECT * FROM sift_ns.sift1m WHERE id = 500000;
```

| 排名 | 占比 | Shared Object | 函数 | 分类 |
|:----:|:----:|---------------|------|:----:|
| | | | | |

---

## 二、热点函数分类

<!-- 把所有场景的 >0.5% 函数汇总，按 Shared Object 归类 -->

### A. Parquet 数据解码 → libiceberg_rust_bridge.so

| 函数 | IVF K=10 | 全扫 K=10 | 作用 |
|------|:--------:|:---------:|------|
| `RleDecoder::get_batch_with_dict` | | | RLE + bit-packing 解码 |
| `DefLevelBufferDecoder::read_def_levels` | | | 解析 Definition Level（NULL 标记） |
| `RepetitionLevelDecoder::read_rep_levels` | | | 解析 Repetition Level（嵌套层级） |
| `ListArrayReader::consume_batch` | | | 组装 Arrow ListArray |
| `DefinitionLevelDecoderImpl::read_def_levels` | | | Def Level 另一种实现 |

### B. Parquet 底层工具 → libiceberg_rust_bridge.so

| 函数 | IVF K=10 | 全扫 K=10 | 作用 |
|------|:--------:|:---------:|------|
| `parquet::util::bit_pack::unpack32::unpack` | | | bit-packing 解包 |
| `ZSTD_decompressSequences` | | | Zstd 解压 |
| `RleDecoder::get_batch` (无 dict) | | | 无字典 RLE |

### C. 内核 → [unknown]

| 函数 | IVF K=10 | 全扫 K=10 | Btree |
|------|:--------:|:---------:|:-----:|
| `[kernel] 0xffffffff9a201174` | | | |

### D. gaussdb 基础设施 → gaussdb

| 函数 | IVF K=10 | Btree | 作用 |
|------|:--------:|:-----:|------|
| `undo::InitUndoZoneLock()` | | | undo 日志锁初始化 |
| `LWLockRelease()` | | | 轻量级锁释放 |
| `hash_search_with_hash_value()` | | | 共享内存哈希查找 |

### E. 向量索引搜索 → libiceberg_rust_bridge.so

<!-- perf report ... | grep -v parquet | grep libiceberg_rust_bridge | grep -iE "ivf|index|distance|scan" -->

| 函数 | IVF K=10 | 作用 |
|------|:--------:|------|
| `IcebergTableReader::read_file_rows_addressed` | | 按索引地址读取行 |
| `IcebergTableReader::scan_single_file` | | 单文件扫描 |

---

## 三、综合对比

### 3.1 按模块汇总

| 模块 | 全扫 K=10 | IVF K=10 | Btree 点查 |
|------|:---------:|:--------:|:----------:|
| Parquet 数据解码 (A) | | | |
| Parquet 底层工具 (B) | | | |
| 内核 [unknown] (C) | | | |
| gaussdb 基础设施 (D) | | | |
| 向量索引搜索 (E) | — | | — |
| tokio 异步 | | | — |
| 其他/未归类 | | | |

### 3.2 可视化

```
全扫 2.4s (1M 行)         IVF K=10 0.8s (~976 行)       Btree 0.18s (1 行)
────────────────────       ───────────────────────       ─────────────────────
Parquet解码 ██████         Parquet解码 ██████             gaussdb内部 ██████
内核unknown █████          内核unknown █████              Parquet解码 ████
gaussdb内部 ██             gaussdb内部 ██                内核unknown ███
其他        ██████         索引搜索    ▏                  其他        ██████
                           其他        ██████
────────────────────       ───────────────────────       ─────────────────────
```

### 3.3 关键发现

<!-- 根据数据，回答以下问题：-->
<!-- 1. 全扫和 IVF 热点是否相同？为什么？ -->
<!-- 2. 向量索引搜索开销具体多少？ -->
<!-- 3. 加速比 vs 理论值的差距由什么因素解释？ -->
<!-- 4. Btree 为什么不同于向量查询？ -->
<!-- 5. 与之前测试相比有什么变化？ -->

---

## 四、总结与优化建议

| 优先级 | 方向 | 依据 | 适用场景 |
|:------:|------|------|:--------:|
| P0 | | | |
| P1 | | | |
| P2 | | | |

## 五、火焰图文件

<!-- ls -lh flamegraphs/ -->

| 文件 | 样本 | 大小 |
|------|:----:|:----:|
| `flame_fullscan_k10_YYYY-MM-DD.svg` | | |
| ... | | |
