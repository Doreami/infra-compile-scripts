# {数据集名称} 索引性能测试报告

**测试日期**: YYYY-MM-DD
**编译模式**: Release + 帧指针 (`-fno-omit-frame-pointer` / `force-frame-pointers=yes`)
**测试环境**: openEuler 24.03, 96核, 723GB, kernel 6.6.0-28

---

## 一、版本信息

| 组件 | Commit | 说明 |
|------|--------|------|
| openGauss-server-datainfra | `XXXXXXX` (YYYY-MM-DD) | |
| iceberg-rust-bridge | `XXXXXXX` (YYYY-MM-DD) | |
| iceberg-index | `XXXXXXX` (YYYY-MM-DD) | |
| iceberg-rust-cache | `XXXXXXX` (YYYY-MM-DD) | |
| iceberg-rust-datainfra | `XXXXXXX` (YYYY-MM-DD) | |
| openGauss-Catalog | `XXXXXXX` (YYYY-MM-DD) | |
| iceberg_fdw | `XXXXXXX` (YYYY-MM-DD) | |

---

## 二、数据源

| 项目 | 值 |
|------|---|
| 数据集 | {SIFT1M / GIST1M / ...} |
| 向量维度 | {128 / 960 / ...} |
| 数据量 | {1,000,000} 行 |
| 查询集 | {10,000 / 1,000} 条 |
| Ground Truth | Top-{100} ({ivecs / HDF5}) |
| 存储格式 | Parquet ({10} 文件, 每文件 {100K} 行) |

### 表结构

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| vec | vector({N}) | {N} 维 float32 向量 |

### 索引

| 索引名 | 类型 | 列 | 状态 |
|--------|------|---|------|
| idx_btree_id | btree ({key_column}) | id | active |
| idx_ivf_vec | ivf_flat (num_clusters={1024}) | vec | active |

> **建索引耗时**: {XX} 分钟（nohup + time 记录）

---

## 三、建表 & 建索引 SQL

### 建命名空间
```sql
SELECT iceberg_catalog.create_namespace('{ns}', '{}'::jsonb);
```

### 建表
```sql
SELECT iceberg_catalog.create_table('{ns}', '{table}',
  '{"type":"struct","fields":[
    {"id":1,"name":"id","type":"long","required":true},
    {"id":2,"name":"vec","type":{"type":"list","element-id":3,"element":"float","element-required":true},"required":false}
  ]}'::jsonb
);
```

### Btree 索引
```sql
SELECT iceberg_catalog.create_index(
    '{ns}', '{table}', 'idx_btree_id',
    '["id"]'::jsonb, 'btree', 'btree',
    '{"key_column":"id"}'::jsonb
);
```

### IVF 向量索引
```sql
SELECT iceberg_catalog.create_index(
    '{ns}', '{table}', 'idx_ivf_vec',
    '["vec"]'::jsonb, 'ivf_flat', 'ivf',
    '{"num_clusters":{1024}, "sample_rate":{100000}}'::jsonb
);
```

---

## 四、测试结果

- 耗时来源: `EXPLAIN (ANALYZE)` 的 `Total runtime`
- 每组 3 次取平均（IVF 第一轮冷启动不计入）
- 全扫: `SET enable_indexscan = off; SET enable_bitmapscan = off; SET enable_vectorsearch = off;`
- IVF: `SET enable_vectorsearch = on; SET try_vector_engine_strategy = force;`

### K 值曲线

| K | 全扫 (ms) | IVF (ms) | 加速比 |
|---|-----------|----------|--------|
| 10 | | | |
| 50 | | | |
| 100 | | | |
| 500 | | | |
| 1000 | | | |
| 5000 | | | |
| 10000 | | | |

### nprobe 对比（可选）

| nprobe | K=10 耗时 | Recall@10 |
|--------|-----------|:---------:|
| 1 | | |
| 4 | | |
| 8 | | |
| 16 | | |
| 32 | | |

---

> **耗时来源**：使用 `EXPLAIN (ANALYZE)` 的 `Total runtime`（非 wall time，非节点 actual time）。首次 IVF 查询为冷启动（含索引加载），不计入平均。

## 五、EXPLAIN ANALYZE 输出（代表性场景）

```
-- IVF K=10
{粘贴 EXPLAIN ANALYZE 输出}
```

```
-- 全扫 K=10
{粘贴 EXPLAIN ANALYZE 输出}
```

---

## 六、与历史对比

| 数据集 | 维度 | IVF K=10 | 全扫 K=10 | 加速比 |
|--------|:---:|:--------:|:---------:|:------:|
| SIFT1M (YYYY-MM-DD) | 128 | | | |
| GIST1M (YYYY-MM-DD) | 960 | | | |
| **本次** | | | | |

---

## 七、总结

1. 核心发现
2. 与历史对比
3. 异常/注意事项
