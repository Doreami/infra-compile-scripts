# {数据集名称} 火焰图分析 — YYYY-MM-DD

## 版本信息

| 组件 | Commit | 说明 |
|------|--------|------|
| openGauss-server-datainfra | `XXXXXXX` (YYYY-MM-DD) | |
| iceberg-rust-bridge | `XXXXXXX` (YYYY-MM-DD) | |
| iceberg-index | `XXXXXXX` (YYYY-MM-DD) | |
| iceberg-rust-cache | `XXXXXXX` (YYYY-MM-DD) | |
| iceberg-rust-datainfra | `XXXXXXX` (YYYY-MM-DD) | |
| openGauss-Catalog | `XXXXXXX` (YYYY-MM-DD) | |
| iceberg_fdw | `XXXXXXX` (YYYY-MM-DD) | |

## 采集信息

| 项目 | 值 |
|------|---|
| 采样工具 | `perf record -F 99 -g -p <gaussdb PID>` |
| 编译模式 | Release + 帧指针 |
| 数据集 | {SIFT1M / GIST1M}, {1M} 行 × {128/960} 维 float |
| 向量索引 | ivf_flat, num_clusters={1024} |
| Btree 索引 | btree, key_column=id |

## 场景覆盖

| 场景 | 样本数 | 策略 | 说明 |
|------|:-----:|------|------|
| `fullscan_k10` | | | 全扫 LIMIT 10 |
| `fullscan_k100` | | | 全扫 LIMIT 100 |
| `fullscan_k1000` | | | 全扫 LIMIT 1000 |
| `ivf_k10` | | | IVF K=10 |
| `ivf_k100` | | | IVF K=100 |
| `ivf_k10000` | | | IVF K=10000 |
| `btree_point` | | | Btree 点查 |

## 耗时基准

| 场景 | 平均(warm) |
|------|:---------:|
| 全扫 K=10 | |
| IVF K=10 | |
| Btree 点查 | |

---

## 一、逐场景详细数据

### 1.1 全扫 K=10

```sql
SET enable_indexscan = off; SET enable_bitmapscan = off; SET enable_vectorsearch = off;
SELECT id FROM {ns}.{table} ORDER BY vec <-> '[...]'::vector LIMIT 10;
```

| 排名 | 占比 | Shared Object | 函数 | 分类 |
|:----:|:----:|---------------|------|:----:|
| 1 | | | | |

### 1.2 全扫 K=100

### 1.3 全扫 K=1000

### 1.4 IVF K=10

```sql
SET enable_vectorsearch = on; SET try_vector_engine_strategy = force;
SELECT id FROM {ns}.{table} ORDER BY vec <-> '[...]'::vector LIMIT 10;
```

| 排名 | 占比 | Shared Object | 函数 | 分类 |
|:----:|:----:|---------------|------|:----:|
| 1 | | | | |

> 调用栈追溯（展开 Top 函数）

### 1.5 IVF K=100

### 1.6 IVF K=10000

### 1.7 Btree 点查

```sql
SELECT * FROM {ns}.{table} WHERE id = 500000;
```

---

## 二、热点函数分类

### A. Parquet 数据解码 → libiceberg_rust_bridge.so

| 函数 | IVF K=10 | 全扫 K=10 | 全扫 K=1000 | 作用 |
|------|:--------:|:---------:|:-----------:|------|

### B. Parquet 底层工具 → libiceberg_rust_bridge.so

### C. 内核 → [unknown]

| kernel symbol | IVF K=10 | IVF K=100 | 全扫 K=10 | Btree |
|---------------|:--------:|:---------:|:---------:|:-----:|

### D. gaussdb 基础设施 → gaussdb

### E. 向量索引搜索 → libiceberg_rust_bridge.so

---

## 三、综合对比

### 3.1 按模块汇总

| 模块 | 全扫 K=10 | IVF K=10 | IVF K=100 | Btree 点查 |
|------|:---------:|:--------:|:---------:|:----------:|

### 3.2 可视化

```
全扫 K=10           IVF K=10            Btree 点查
================     ================     ================
```

### 3.3 关键发现

1. 全扫和 IVF 热点是否相同？
2. 向量索引搜索 CPU 开销？
3. 加速比差距的根因？
4. Btree 热点为什么不同？

### 3.4 {数据集} vs 历史对比

| 模块 | 上次 ({数据集}) | 本次 ({数据集}) | 变化 |
|------|:---------:|:---------:|:----:|

---

## 四、优化建议

| 优先级 | 方向 | 依据 | 适用场景 |
|:------:|------|------|:--------:|
| **P0** | | | |
| **P1** | | | |
| **P2** | | | |

---

## 五、火焰图文件

| 文件 | 样本 | 大小 | 说明 |
|------|:----:|:----:|------|

> 原始 perf data 在 `perf_data/`（已加入 .gitignore，不提交）。
