# 为什么不用 iceberg_catalog.create_table()建表

 那个函数只能建空表结构。import_csv.py 还需要写入 Parquet 数据、记录准确的 metadata_location 和 snapshot_id，所以直接操作 catalog 表来注册元数据。如果用 create_table() 建完后再手动 UPDATE


catalog 表反而多一步，不如一步到位。


