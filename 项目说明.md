# openGauss Iceberg 联合编译环境搭建

一键搭建 openGauss + Iceberg 多仓联合编译环境，产出带 Iceberg FDW / Catalog / Delta / Rust Bridge 的 openGauss 系统。

## 适用环境

- **OS**: openEuler 22.03 / 24.03
- **架构**: x86_64
- **磁盘**: 建议 50GB+ 可用空间
- **内存**: 建议 8GB+

## 快速开始

### 1. 准备 binarylibs

从 openGauss 发布页下载 `openGauss-third_party_binarylibs_openEuler_x86_64.tar.gz`，解压到目标目录：

```bash
mkdir -p ~/iceberg-og
cd ~/iceberg-og
tar xzf /path/to/openGauss-third_party_binarylibs_openEuler_x86_64.tar.gz
mv openGauss-third_party_binarylibs_openEuler_x86_64 binarylibs
```

### 2. 配置 GitHub 认证（可选但推荐）

如果服务器无法匿名访问 GitHub，创建 [Personal Access Token (classic)](https://github.com/settings/tokens)，勾选 `public_repo`：

```bash
# 编辑 config.env 或在终端设置:
export GITHUB_USER="your_username"
export GITHUB_TOKEN="ghp_xxxx"
```

### 3. 一键执行

```bash
cd infra-compile-scripts
bash setup.sh
```

脚本会自动完成：

1. ✅ 环境检查 + 依赖安装提示
2. ✅ 克隆 6 个仓库
3. ✅ 创建编译配置文件
4. ✅ 安装 Rust 1.96.0（国内镜像加速）
5. ✅ 平台兼容处理（自动 patch boost ABI 问题）
6. ✅ 编译 openGauss (debug 模式, 30-60 分钟)
7. ✅ 编译 Rust bridge
8. ✅ 编译 FDW / Catalog / Delta
9. ✅ 验证安装

## 产出的编译产物

```
~/iceberg-og/
├── binarylibs/                          # openGauss 三方库
├── openGauss-server-datainfra/
│   └── mppdb_temp_install/              # GAUSSHOME
│       ├── bin/gaussdb, gsql, pg_config
│       └── lib/postgresql/
│           ├── libiceberg_rust_bridge.so
│           ├── iceberg_fdw.so
│           ├── iceberg_catalog.so
│           ├── iceberg_delta.so
│           └── proc_srclib/             (副本)
├── iceberg-index/
├── iceberg-rust-bridge/
├── iceberg_fdw/
├── openGauss-Catalog/
└── iceberg_delta/
```

## 单仓编译

使用 `build.sh` 单独编译某个仓，自动处理环境变量和依赖检查。无需先跑 `setup.sh`，可直接从 0 开始逐个编译：

```bash
# 查看帮助
bash build.sh --help

# 从 0 开始按依赖顺序编译（--force = 全量，不加 = 增量）
bash build.sh opengauss --force         # 1. openGauss（30-60 分钟）
bash build.sh bridge --force            # 2. iceberg-index + bridge
bash build.sh fdw --force               # 3. fdw
bash build.sh catalog --force           # 4. catalog
bash build.sh delta --force             # 5. delta

# 日常增量编译
bash build.sh fdw                       # 只编 fdw
bash build.sh bridge --release          # release 模式
bash build.sh index                     # cargo check（仅语法检查）

# --force: 先 clean 再全量编译
bash build.sh fdw --force            # make clean + make
bash build.sh bridge --force         # cargo clean + cargo build
bash build.sh delta --force          # rm -rf build + cmake configure + build

# --pull: 编译前拉取最新代码
bash build.sh fdw --pull             # git pull + 增量编译
bash build.sh bridge --pull          # git pull iceberg-index + bridge + 增量编译
```

> bridge/fdw/catalog/delta 不加 `--force` 走增量编译（make / cargo / cmake 自带增量检测）。  
> opengauss 仅全量编译：产物存在 + 模式匹配则跳过，否则重编。

**依赖链**：`opengauss` → `bridge` → `catalog` → `delta`，`fdw` 只需 `opengauss`。  
脚本会自动检查前置依赖是否存在，缺失时会报错提示。

### Fork 仓配置

`config.env` 支持两种 fork 方式：

```bash
# 全部 fork — 只改 GITHUB_ORG
GITHUB_ORG="your_username"

# 单个 fork — 覆盖对应 _REPO_URL
ICEBERG_FDW_REPO_URL="https://github.com/your_fork/iceberg_fdw.git"
ICEBERG_CATALOG_REPO_URL="https://github.com/your_fork/openGauss-Catalog.git"
```

### 手动重编（备选）

```bash
# 环境变量
source ~/iceberg-og/opengauss.env
export GAUSSHOME=~/iceberg-og/openGauss-server-datainfra/mppdb_temp_install
GCC_HOME=~/iceberg-og/binarylibs/buildtools/gcc10.3/gcc
export PATH=$GCC_HOME/bin:$GAUSSHOME/bin:$PATH

# 只重编 bridge
cd ~/iceberg-og/iceberg-rust-bridge
cargo build --release [--config ...]
cp target/release/libiceberg_rust_bridge.so $GAUSSHOME/lib/postgresql/

# 只重编 fdw
cd ~/iceberg-og/iceberg_fdw
make && make install PG_CONFIG=$GAUSSHOME/bin/pg_config
cp iceberg_fdw.so $GAUSSHOME/lib/postgresql/

# 只重编 catalog
cd ~/iceberg-og/openGauss-Catalog
make GAUSS_SRC=~/iceberg-og/openGauss-server-datainfra
cp iceberg_catalog.so $GAUSSHOME/lib/postgresql/

# 只重编 delta
cd ~/iceberg-og/iceberg_delta/tmp_build_gcc10
cmake --build . --parallel 8
cp iceberg_delta.so $GAUSSHOME/lib/postgresql/
```

## 已知问题 & 解决

### 1. `libboost_thread.so: undefined reference to GLIBCXX_3.4.30`

**原因**: 系统 boost 用 GCC12 编译，与编译用的 GCC10.3 libstdc++ ABI 不兼容。

**解决**: 脚本自动从 binarylibs 的 `.a` 生成 `.so` 并 patch 构建文件。（`setup.sh` 第 5 步）

### 2. `cmake: version GLIBCXX_3.4.29 not found`

**原因**: cmake 是系统 GCC12 编译的，需要系统 libstdc++。

**解决**: cmake 步骤使用 `LD_LIBRARY_PATH=/usr/lib64:...` 优先系统库。（`setup.sh` 第 8d 步）

### 3. `make install` JRE 文件权限错误

**原因**: 已安装的 `$GAUSSHOME/jre` 下文件为 `0444`。

**解决**: `chmod -R u+w $GAUSSHOME/jre`。（`setup.sh` 第 6 步自动执行）

### 4. GitHub 无法连接 / 要求认证

**解决**: 创建 GitHub Classic PAT，设置 `GITHUB_USER` 和 `GITHUB_TOKEN`。脚本会自动配置 `~/.git-credentials`。

### 5. Rust 工具链下载慢

**解决**: 设置 `RUSTUP_DIST_SERVER` 使用国内镜像。脚本默认使用 `rsproxy.cn`，可在 `config.env` 中修改。

### 6. `make: <g++>: No such file or directory` (Error 127)

**原因**: `~/.bashrc` 中 `CC`/`CXX` 环境变量指向了旧的或不存在路径，覆盖了脚本和 Makefile 的编译器检测。编译到 libpq 等子模块时可能随机报 Error 127。

**解决**: 检查 `~/.bashrc` 中是否有类似以下旧配置，将其删除或注释：
```bash
# 如果有指向旧路径的 CC/CXX/GAUSSHOME，删掉它们
export CC=/path/to/old/gcc     # ← 删除
export CXX=/path/to/old/g++    # ← 删除
```
`setup.sh` 和 `build.sh` 会自动管理编译器路径，不需要在 `.bashrc` 中手动设置。

## 运行时验证

```bash
export GAUSSHOME=~/iceberg-og/openGauss-server-datainfra/mppdb_temp_install
export LD_LIBRARY_PATH=$GAUSSHOME/lib:$GAUSSHOME/lib/postgresql:$LD_LIBRARY_PATH

# 启动 openGauss
$GAUSSHOME/bin/gs_initdb -D /tmp/ogdata
$GAUSSHOME/bin/gaussdb -D /tmp/ogdata

# 连接并创建扩展
$GAUSSHOME/bin/gsql -d postgres -c "CREATE EXTENSION iceberg_fdw;"
$GAUSSHOME/bin/gsql -d postgres -c "CREATE EXTENSION iceberg_catalog;"
$GAUSSHOME/bin/gsql -d postgres -c "CREATE EXTENSION iceberg_delta;"
```

## 目录结构

```
infra-compile-scripts/
├── README.md       # 快速开始
├── 项目说明.md      # 本文档（详细说明）
├── config.env      # 可配置变量（路径、镜像、GitHub 认证等）
├── setup.sh        # 一键搭建脚本（clone + 编译全家桶）
└── build.sh        # 单仓编译脚本（无需 setup.sh，可直接使用）
```
